# Zayed POS Local Test Fixture

This folder contains a copied Zayed POS database/log set from the branch issue around 2026-07-19 and 2026-07-20.

Use this data only for local forensic testing. Do not place `warehouse_data.sqlite3` directly under `POS/`, and do not run the normal POS app against it unless sync is disabled or the DB is copied to a temporary location first.

Copied files:
- `warehouse_data.sqlite3`
- `HosnyPOS-ZAY.log`

